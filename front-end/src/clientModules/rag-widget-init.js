// Initialize the RAG chat widget when the page loads\nif (typeof window !== 'undefined') {\n  // Wait for the DOM to be fully loaded\n  if (document.readyState === 'loading') {\n    document.addEventListener('DOMContentLoaded', initWidget);\n  } else {\n    // DOM is already loaded, call initWidget directly\n    initWidget();\n  }\n}\n\nfunction initWidget() {\n  // Check if the widget initialization function is available\n  if (window.initRagChatWidget) {\n    // Initialize the widget with configuration\n    // NOTE: Update this URL to point to your deployed RAG API\n    // For production: 'https://your-deployment-url.com'\n    // For testing with local API: 'http://localhost:8000'\n    window.initRagChatWidget({\n      backendUrl: 'http://localhost:8000', // Update this to your deployed API URL\n      title: 'Book Assistant',\n      initialMessage: 'Ask me anything about robotics!',\n      position: 'bottom-right',\n      welcomeMessage: 'Hello! I\\'m your Robotics Knowledge Assistant. Ask me anything about the content on this page!'\n    });\n  } else {\n    // If the widget script hasn\\'t loaded yet, try again after a short delay\n    setTimeout(initWidget, 100);\n  }\n}